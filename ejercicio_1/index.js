const cheerio = require("cheerio");

//! funcion autoejecutable anonima
/*(() => {
  console.log("hola me auto ejecute");
})();

const number = [0, 1, 2, 3, 4];

number.map((num) => {
  console.log(num);
});

const result = number.filter((element)=> element<3);
console.log(result)
*/

